import type { Character } from '../data/frierenData';
export default function CharacterCard({character:c}:{character:Character}){return <article className="character-card"><div className="character-portrait" style={{'--portrait-color':c.color} as React.CSSProperties} role="img" aria-label={`Placeholder potret ${c.name}`}><span className="portrait-orbit"/><span className="portrait-letter">{c.initials}</span><span className="portrait-tag">{c.party==='Pahlawan'?'THE ORIGINAL PARTY':'A NEW CHAPTER'}</span><span className="portrait-spark">✧</span></div><div className="character-info"><h3>{c.name}</h3><small>{c.role}</small><p>“{c.quote}”</p></div></article>}
