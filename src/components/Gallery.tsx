import { useEffect, useRef, useState } from 'react';
import { ArrowUpRight, ChevronLeft, ChevronRight, X } from 'lucide-react';
import { memories } from '../data/frierenData';
export default function Gallery(){const [selected,setSelected]=useState<number|null>(null);const dialog=useRef<HTMLDialogElement>(null);const trigger=useRef<HTMLButtonElement|null>(null);const close=()=>{dialog.current?.close();setSelected(null);trigger.current?.focus()};useEffect(()=>{if(selected!==null&&!dialog.current?.open)dialog.current?.showModal()},[selected]);useEffect(()=>{if(selected===null)return;const previous=document.body.style.overflow;document.body.style.overflow='hidden';return()=>{document.body.style.overflow=previous}},[selected]);const move=(step:number)=>setSelected(s=>s===null?null:(s+step+memories.length)%memories.length);return <section id="memories" className="section page-width"><div className="section-top"><div><div className="eyebrow">04 — POTONGAN KENANGAN</div><h2>Beberapa momen <em>tak pernah pudar.</em></h2></div><p className="section-aside">Tempat baru. Langit yang sama.<br/>Dan cerita yang terus kita bawa.</p></div><div className="gallery-grid">{memories.map((m,i)=><button className="memory" key={m.id} onClick={e=>{trigger.current=e.currentTarget;setSelected(i)}} aria-label={`Lihat ${m.title}`}><img src={m.image} alt={m.alt} loading="lazy"/><div className="memory-overlay"><small>{m.location}</small><h3>{m.title}</h3></div><span className="memory-open"><ArrowUpRight size={18}/></span></button>)}</div><p className="placeholder-note">Foto suasana sebagai placeholder • Kenangan menunggu untuk diisi</p><dialog ref={dialog} className="lightbox" onCancel={e=>{e.preventDefault();close()}} onClick={e=>{if(e.target===e.currentTarget)close()}} onKeyDown={e=>{if(e.key==='ArrowLeft')move(-1);if(e.key==='ArrowRight')move(1)}} aria-label="Galeri kenangan">{selected!==null&&<div className="lightbox-content"><button autoFocus className="lightbox-close icon-button" onClick={close} aria-label="Tutup galeri"><X/></button><img src={memories[selected].image} alt={memories[selected].alt}/><div className="lightbox-bottom"><button className="icon-button" onClick={()=>move(-1)} aria-label="Foto sebelumnya"><ChevronLeft/></button><div><h3>{memories[selected].title}</h3><small>{selected+1} / {memories.length}</small></div><button className="icon-button" onClick={()=>move(1)} aria-label="Foto berikutnya"><ChevronRight/></button></div></div>}</dialog></section>}
